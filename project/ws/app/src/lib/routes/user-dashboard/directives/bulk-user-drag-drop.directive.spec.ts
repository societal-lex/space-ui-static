import { BulkUserDragDropDirective } from './bulk-user-drag-drop.directive';

describe('BulkUserDragDropDirective', () => {
  it('should create an instance', () => {
    const directive = new BulkUserDragDropDirective();
    expect(directive).toBeTruthy();
  });
});
